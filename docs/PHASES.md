# AI Trading System — 22-Phase Dashboard Build Plan

> Progress tracker for the dashboard build. Update this file as phases complete.
> Last updated: 2026-08-21

## Status Legend
- [x] Done
- [ ] Pending

---

## Phase Overview

- [x] **Phase 1 — Foundation:** FastAPI backend scaffold, SQLite DB, JWT auth system
- [x] **Phase 2 — UI/UX Design:** Wireframes, dark-theme design system (`globals.css`)
- [x] **Phase 3 — Frontend Component Build:** Next.js + TypeScript monorepo, component library (UI/charts/tables/trade), responsive mobile interface, all 12 pages using demo data
- [x] **Phase 4 — Auth + Dashboard Shell + DB Working:** Register/login/JWT, AuthGuard route protection, AppShell layout, overview page wired to real backend, DB auto-creates tables
- [x] **Phase 5 — DB ↔ Backend + Overview + Signals:** *(completed 2026-08-20)* Services layer (`signal_service`, `trade_service`), TTL cache, per-tier rate limiting (free=30/min, premium=120/min, vip=300/min), `/api/engine/scan` + `/api/engine/status`, signals stored from live engine runs, tier-filtered listings. Verified end-to-end: scan → store → filter → cache → rate limit.
- [x] **Phase 6 — Live Market Data:** *(completed 2026-08-20)* `backend/services/market_service.py` — live quotes via Alpaca (stocks: SPY/QQQ/DIA + top movers from 20-symbol watchlist) and Twelve Data (crypto/forex/gold), parallel TD fetches, OHLC bars endpoint for charts, crypto discovery (cached scans). Endpoints: `/api/market/overview|quotes|movers|bars|discovery`. All TTL-cached (quotes 60s, bars 300s) to protect the 8-credits/min Twelve Data budget. Frontend market page wired live with 60s auto-refresh. Verified: cold ~1-2s (up to 20s when TD throttled), warm 30ms.
- [x] **Phase 7 — AI Signals Page + Live Feed:** *(completed 2026-08-20)* Signal detail endpoint + modal (indicators, entry/exit levels), confidence decay (12h half-life, computed at read time), SSE live feed `/api/signals/feed` with tier gating (token via query param for EventSource), historical accuracy endpoint (`/api/signals/accuracy`) grouping closed trades by strategy+market. Frontend signals page fully live: filters, decay bars, tier badges, EventSource feed with toast, accuracy table. Seeded 14 closed trades for accuracy stats.
- [x] **Phase 8 — Positions + Portfolio:** *(completed 2026-08-20)* New endpoints `/api/portfolio/positions` (open trades + live price + unrealized P&L), `/holdings` (per-symbol net qty/avg entry/market value), `/performance` (equity curve from closed trades, win rate, max drawdown, allocation by market). Live quotes fetched in parallel threads with 10s per-quote timeout degrading to `null` (protects against Twelve Data throttling / Alpaca outages). Trades page live (positions grid + real invested/unrealized stats); portfolio page live (equity LineChart, allocation DonutChart, DataTable history). Seeded 5 open positions.
- [x] **Phase 9 — Trading Page + Performance Charts & Analytics:** *(completed 2026-08-21)* New `backend/api/trading.py`: `POST /api/trading/orders` (paper fill at live or provided price), `POST /api/trading/close/{id}` (live/explicit exit price, ownership-enforced), `GET /api/trading/analytics` (daily P&L series, cumulative equity, drawdown series, win rate by strategy, market correlation matrix via numpy; `days`/`start`/`end` range params), `GET /api/trading/export` (CSV attachment). New `backend/services/analytics_service.py`. Frontend: new `/trading` page (order form, close buttons, recent orders), new `/analytics` page (equity/daily-P&L/drawdown/win-rate charts, 7D/30D/90D/All range switcher, correlation table, CSV download), home P&L placeholder now wired to real equity curve, sidebar links added. PDF export deferred (needs reportlab dep) — CSV covers the workflow.
- [x] **Phase 10 — Risk Management:** *(completed 2026-08-21)* `backend/api/risk.py` wired to `risk/central_risk.py` (shared cross-market engine, thread-safe, file-persisted state): `/api/risk/status` (user metrics + engine summary), `/exposure`, `PUT /limits`, `POST /kill-switch` (user-level always; admin releases shared engine switch), `POST /reset-daily` (admin). `risk_service.pre_trade_check` enforced in `POST /api/trading/orders` (kill switch, max positions, daily loss, position size, exposure cap) — central engine records open/close on every fill. Frontend risk page live: editable limits form, kill switch toggle with confirmation state, daily-loss + drawdown progress bars (auto-trip at 10% cap), exposure-by-market chart, cross-market engine panel (daily P&L, positions per market, trades today, kill-switch badge). Per-user **max daily trades** limit (`risk_settings.max_daily_trades`, default 20, clamped 1-200): SQLite auto-migration adds the column on boot, `pre_trade_check` blocks orders once `trades_today >= limit` (counts all trades opened since UTC midnight), exposed in `/status` as `trades_today`/`max_daily_trades`, editable in the limits form; feeds the composite risk score. Verified end-to-end 43/43: all gates return 403 with reasons, limits persist, invalid limits rejected (422), unauth rejected (401).
- [x] **Phase 11 — AI + Exchanges:** *(completed 2026-08-21)* New `backend/services/ai_service.py` + `backend/api/ai.py`: `GET /api/ai/analysis?symbol=&market=` runs the core system's models (MarketRegimeDetector, MomentumDecoder, ConfidenceScorer) over live daily bars (TTL-cached 300s, off event loop via to_thread; R^2-based trend strength feeds the regime detector); `GET /api/ai/summary` analyzes SPY/BTC-USD/EUR-USD per market; `GET /api/ai/models` roster. New `backend/api/exchanges.py`: connect/list/disconnect/test — keys Fernet-encrypted at rest (`security/security_manager.py`, ENCRYPTION_KEY), masked to last 4 chars in all responses, one connection per exchange, ownership-enforced, test pings Alpaca account endpoint and sets is_active. Fixed `_get_master_fernet` key-rotation bug (dotenv placeholder won first-wins → new key every boot would orphan encrypted data; now parses .env last-wins and replaces in place). SQLite auto-migration adds `exchange_connections.last_checked`. Frontend: `/ai-analysis` live (per-market regime cards, symbol analyzer with momentum bars/confidence breakdown/model roster), `/exchanges` live (connect form, status badges validated/failed/untested, test + disconnect actions). Verified end-to-end 30/30.
- [x] **Phase 12 — Trading History, Payments & Invoices:** *(completed 2026-08-21)* DB-backed billing: new `Invoice`/`Subscription`/`PromoCode` models; `backend/services/billing_service.py` + `backend/api/billing.py`. Pricing/wallets stay sourced from `payments/payment_manager.py` (Telegram bot shares one truth). User: plans, subscription state (lazy expiry sweep downgrades tier), auto-renew toggle, create invoice (crypto USDT w/ wallet address, or manual), submit payment proof, cancel own pending. Admin: pending queue w/ approve (creates/extends/replaces subscription — same-tier renewals stack remaining time, upgrades swap immediately, user.tier synced), reject, refund (cancels sub + downgrades to free), promo CRUD (pct discount, max uses, usage counting), revenue stats. `GET /api/trading/history`: filters market/symbol/strategy/outcome(win|loss|open)/date-range + pagination + per-page summary. Naive-UTC convention used throughout billing (SQLite drops tzinfo). Frontend: `/history` live (filter bar, stats, paginated table), new `/billing` page (subscription card, plans, invoice form w/ promo, invoices table with proof submission/cancel, admin panel: stats/promos/all-invoices approve-reject-refund). Sidebar Billing link added. Verified end-to-end 43/43.
- [x] **Phase 13 - Telegram Integration:** *(completed 2026-08-21)* Per-user, preference-gated Telegram dispatch. Users link their chat ID (`users.telegram_chat_id`, SQLite migration) via `/api/preferences/telegram/link|test|unlink`; linking fires a live test message. `telegram_service.py`: category gates (signals/tp_sl/market_analysis/risk/system) x per-user `UserPreferences` toggles x tier gating (free = signals+system only), Bot API sends off-loop via `asyncio.to_thread`, never raises into callers; `broadcast()` fans out by user default_market with fallback to all. Hooks: engine scan -> signal alerts broadcast; position close -> TP/SL alert; kill-switch -> risk alert; invoice approve/reject/refund -> system alert to owner. `/api/preferences` GET/PUT with enum validation. Settings page now live: profile card, Telegram linking card (status badge, link/test/unlink, 5 persisted toggle switches), general prefs saved server-side. Verified 25/25.
- [x] **Phase 14 - Exchange Adapter Registry:** *(completed 2026-08-21)* Pluggable adapter architecture in ackend/exchanges/: BaseAdapter contract (validate/account) + 4 adapters - Alpaca (alpaca-py SDK), Kraken (stdlib HMAC-SHA512 signing), OANDA v20 (bearer REST, practice/live hosts via is_paper), Coinbase Advanced Trade (hand-rolled ES256 JWT over cryptography lib, DER->r||s conversion, b64-wrapped PEM accepted; no PyJWT dependency). API refactor: /api/exchanges/meta lists adapters for the UI picker; connect() validates credentials BEFORE persisting (stored inactive on failure with validation detail); /api/exchanges/{id}/account returns live balance snapshot (502 on bad creds); binance removed from supported set. Schema: api_secret now optional (OANDA is token-only). Frontend /exchanges: dropdown + labels driven by /meta, secret field hidden for adapters without one. Verified 26/26 incl. signature/JWT unit checks.
- [x] **Phase 15 - Notification Center:** *(completed 2026-08-21)* `notification_service.py` (create with web_notifications pref gate + never-raises, list w/ unread/type/limit filters, unread count, mark read/all, clear-read) + `/api/notifications` router (list, unread-count for bell polling, read, read-all, DELETE /read). In-app notifications now fire from the same live events as Telegram: position close -> trade, kill switch -> risk, invoice approve/reject/refund -> system (title arg), engine scan -> top signals (confidence >= 60%, capped 5/user). Frontend: /notifications page live (all/unread filter, click-to-read, mark-all, clear-read) and Navbar bell wired to real unread count with 30s polling + latest-5 dropdown. Verified 18/18.
- [x] **Phase 16 - Security, Docker & HTTPS:** *(completed 2026-08-21)* SecurityHeadersMiddleware (nosniff, frame-deny, CSP, HSTS, referrer-policy, no-store on /api/*); brute-force lockout on login (5 failures per email+IP -> 15min, in-memory bucket); production warning when SECRET_KEY left at dev default; CORS already origin-locked to localhost:3000/8000. SQLi audit: zero raw f-string SQL across backend (static scan enforced in tests). JWT tamper rejection verified; HS256 algorithm-pinned decode was already correct. Containerization: Dockerfile.backend (slim, non-root, healthcheck), Dockerfile.frontend (3-stage build), docker-compose.yml (backend+frontend+Caddy proxy, SQLite volume, env-injected secrets), deploy/Caddyfile (auto-HTTPS reverse proxy sample), .dockerignore. CI: .github/workflows/ci.yml (compile+boot check backend, tsc+build frontend, both docker images built on PR). NOTE: docker not installed on this machine - compose stack validated by review only; run `docker compose up` on a host with Docker to verify end-to-end. Verified 16/16 locally.
- [x] **Phase 17 - Testing:** *(completed 2026-08-21)* Repo-rooted pytest suite (`pytest.ini`, `tests/conftest.py` with isolated per-run SQLite DB via DASHBOARD_DATABASE_URL env override, ASGI-transport httpx client, autouse limiter/brute-force reset + kill-switch release between tests). Suites: auth (register/login/me, lockout, tampered JWT, admin guard), trading (open/close with live-price fallbacks, history filters, kill-switch gate, max-daily-trades cap), billing (approve/reject/refund cycle with tier sync, promo discount math, non-admin 403s), notifications+preferences (lifecycle, ownership isolation, validation, telegram link/unlink), exchanges+security unit tests (Kraken HMAC signature, Coinbase ES256 JWT shape, OANDA host selection, security headers, CORS rules). 64 passed. Load harness `tests/load_test.py` (asyncio workers vs live server): 10 workers x 20s = 3,839 req / 0 errors / p50 13ms / p95 19ms / ~192 rps on local uvicorn; Windows loopback drops some initial concurrent connections during the login burst - workers degrade gracefully, non-issue on Linux. CI now runs the suite (requirements-dev.txt). 
- [ ] **Phase 18 — Beta Release:** Deploy beta to production server, invite beta users, monitor logs, collect feedback, crash reporting, rollback plan.
- [ ] **Phase 19 — Production Launch:** Full production launch, monitoring, scaling, support infrastructure, domain setup, DNS config, SSL auto-renewal, backup/restore procedures, incident response playbook.
- [ ] **Phase 20 — Monitoring & Observability:** Prometheus metrics (API latency, error rates, signal speed), Sentry error tracking, uptime monitoring, DB performance monitoring, Telegram bot health checks, alerting rules.
- [ ] **Phase 21 — Data Pipeline & Analytics:** Historical signal performance tracking, user behavior analytics, revenue analytics (MRR, churn, LTV), A/B testing framework for signal strategies, automated daily/weekly reports.
- [ ] **Phase 22 — Offline/PWA & Data Export:** Progressive Web App support (offline mode, service workers), GDPR data export/delete, disaster recovery procedures, data backup automation.

---

## Current Position

- [x] **Order routing through exchange adapters:** *(completed 2026-08-21)* BaseAdapter gained optional `place_order` (ok/broker_order_id/filled_price contract, exceptions captured into ok=False+detail). All four adapters route orders: Alpaca via alpaca-py (market/limit GTC), Kraken AddOrder private HMAC (BTC->XBT pair map, base64 secret), OANDA v3 orders (signed units +/-x100, EURUSD->EUR_USD), Coinbase Advanced ES256 JWT now bound to method+path (product_id mapping BTCUSDT->BTC-USDT, market_market/limit_limit_gtc configs). `POST /api/trading/orders` accepts `order_type` (market|limit), `limit_price`, `route=paper|live`; live resolves the newest active ExchangeConnection serving that market, executes off-loop, records venue + broker_order_id on the trade (new nullable column + migration), and a 502 with broker detail leaves NO trade row. `POST /api/trading/close/{id}` closes live-routed positions at the broker first (opposite-side market) and refuses 502 instead of silently paper-closing if disconnected. Trading page: Route selector (Paper/Live) with per-market venue hint from /api/exchanges, venue shown in fill notice. Tests: tests/test_order_routing.py 12 cases (symbol maps, mocked broker payloads for all adapters, no-connection 403, happy path open+close, rejection leaves no trade, orphaned-live close blocked, paper regression). Suite 76 passed.
- [~] **Phase 20 - Monitoring & Observability:** *(groundwork completed 2026-08-21; Sentry/uptime/DB-perf dashboards deferred to deployment)* Prometheus instrumentation: `backend/monitoring/metrics.py` with ASGI MetricsMiddleware (http_requests_total by method/path/status, http_request_duration_seconds histogram) plus domain counters engine_scans_total, scan_signals_generated_total, scan_errors_total and gauges last_scan_timestamp / last_scan_signals. Public scrape endpoint `GET /metrics` (excluded from instrumentation itself). Readiness probe `GET /readyz` pings the DB (503 on failure); `/api/health` kept. Engine scheduler: scan core extracted from the admin endpoint into `engine_service.run_scan` (identical signal mapping incl. SL/TP/indicators + telegram broadcast + in-app notifications), `services/scheduler.py` asyncio loop started in app lifespan - SCAN_ENABLED=1 default, SCAN_INTERVAL_MINUTES=15, per-(symbol,market,direction) cooldown via SCAN_COOLDOWN_HOURS=4 so autonomous scans never spam repeats. Admin POST /api/engine/scan now delegates to the same service. Tests: tests/test_monitoring.py 9 cases (scrape shape, readyz, record_scan counters, env clamps, injected fake engines: save+fanout shape, cooldown suppression, no-cooldown repeats, engine error captured while others proceed, scheduled tick never raises). Suite 85 passed. Verified live: /readyz up, /metrics exposing request + scan series.

**Rebrand 2026-08-22:** Product renamed to **ORQEVA**. backend APP_NAME -> "ORQEVA" (API docs title + /api/health), frontend metadata title/description, login+register pages (O wordmark, taglines), Sidebar wordmark TradeAI->ORQEVA. Telegram bots keep @muna_* usernames; BotFather display names renamed to ORQEVA Free/Premium/VIP by owner (tokens unchanged). Billing contact @Johnpaulmuna_83 retained.

**Hardening sprint 2026-08-22 (all stages complete, 97 tests green, typecheck OK):**
- Stage 0a - Admin bootstrap: `seed_admin()` applies ADMIN_EMAIL/ADMIN_PASSWORD from env on every boot (idempotent; rotates the password if env changes). .env holds ADMIN_PASSWORD empty by default.
- Stage 0b - Idempotency: `IdempotencyRecord` model (unique user+endpoint+key) + replay window; OrderRequest gained `idempotency_key`; frontend sends `crypto.randomUUID()` per form session so double-clicks never duplicate live orders.
- Stage 1 - Postgres: docker-compose.yml rewritten (db postgres:16-alpine + pg_dump backups every 6h keeping 13; backend DASHBOARD_DATABASE_URL=postgresql+asyncpg://... with POSTGRES_PASSWORD/ADMIN_*/SMTP_*/APP_BASE_URL injected). asyncpg in requirements. Local dev stays SQLite until compose up.
- Stage 2 - Password reset: SMTP config (SMTP_HOST/PORT/USER/PASSWORD/FROM) + email_service.py; PasswordResetToken table (sha256-hashed, 30-min single-use, others invalidated on use); POST /api/auth/forgot always 200 (no enumeration) with fallback delivery to notification center + Telegram when SMTP off; POST /api/auth/reset; brute_force.reset_all on success. Frontend /forgot + /reset pages linked from login.
- Stage 3 - Legal: public /legal/terms|privacy|refunds pages (server components, AppShell public routes); signup consent checkbox (18+, ToS+Privacy+Refunds, signals-not-advice) enforced client-side before register.
- Stage 4 - Protective orders: place_order accepts stop_loss/take_profit on all four adapters — Alpaca native bracket (OrderClass.BRACKET), Kraken close[...] params (stop-loss / take-profit / combo), OANDA stopLossOnFill/takeProfitOnFill, Coinbase bracket_bracket_gtc or standalone stop-limit sibling via _submit_stop. Trade model + migration gained stop_loss/take_profit columns (persisted only for live venues; paper trades stay clean). Trading page shows SL/TP inputs when route=live. Tests: tests/test_protective.py 6 cases (bracket request shapes for each adapter mocked, paper accepts-but-doesn't-route, no persistence on paper).
- Verified live after restart: /readyz {"status":"ready","database":"up"}, /api/health ok, /metrics engine_scans_total climbing (scheduler autonomous).

**Audit fixes 2026-08-22 (100 tests green, typecheck OK):**
- Coinbase SL-only orders made safe: entry is submitted FIRST; the standalone stop-loss sibling is attempted only after a confirmed fill. If the stop fails, the order still succeeds (ok=True) with `protective_error` set — previously a failed pre-entry stop could leave an orphaned trigger order at the broker if the entry then bounced. trading.py surfaces `warning` in the response and files a `trade_warning` in-app notification + Telegram ping telling the user to protect the position manually. Adapter test covers both failure directions (stop fails -> entry kept; entry rejected -> no stop ever placed).
- Dockerfile.backend pins `--workers 1` with an explanatory comment: rate limiting, login lockout, idempotency dedupe and the scan scheduler are single-process state; scaling out requires moving them to shared storage first.
- NOTE: use `%TEMP%\opencode\restart_backend.ps1 -Port 8000` (kill-by-port + WMI-detached launch + curl polls) for restarts — inline Start-Process + Invoke-RestMethod freezes the shell session.

**PAUSED 2026-08-22 - resume checklist:** Phases 1-17 + order routing + Phase 20 groundwork complete (85 tests green: `python -m pytest tests -q -p no:warnings` from repo root). Servers via `%TEMP%\opencode\restart_backend.ps1` / `restart_frontend.ps1` (backend :8001, frontend :3000, login admin@demo.com/adminpass123). Remaining before launch: [1] Phase 21 - analytics pipeline (signal performance tracking, user/revenue analytics MRR-churn-LTV, reports); [2] Phase 22 - PWA + GDPR data export/delete + backup automation; [3] Sentry DSN integration hook (optional init in main.py); [4] Phase 18/19 deployment - NEEDS USER: rent VPS (~$5-10/mo Ubuntu), point domain A-record at it, then docker compose up (Dockerfiles/compose/Caddyfile already in repo); [5] post-deploy: uptime monitoring, DB backups, invite-only registration gate. Telegram bots idle until premium/vip users link. Docker not installed on this dev machine.

Frontend pages still on demo data: none — `notifications` page and Navbar bell were wired to the live API in Phase 15; all other pages went live Phases 6-12.

**Leftover cleanup 2026-08-22:** strong 64-hex SECRET_KEY appended to .env (backend previously booted on the dev default because .env only had JWT_SECRET_KEY, a name config.py never reads). `trade_warning` badge color added to the notifications page map. Still blocked on user: ADMIN_PASSWORD choice for prod (empty locally keeps adminpass123), SMTP credentials for reset emails, legal text review, VPS + domain, Phase 21/22, Sentry DSN.

### Debugging notes (Phase 6)
- If API calls seem slow (~1.1s) from Python test scripts but instant via curl: it's httpx per-client overhead on this machine, not the server. Measure with `curl.exe -w "%{time_total}"`.
- Twelve Data throttles hard when credits are exhausted (calls take up to 15s) — the TTL cache is what protects the dashboard; never bypass it.

### Debugging notes (Phase 7)
- **Never run blocking network I/O in async endpoints** — engine scans froze the whole event loop (SSE starved, all endpoints unresponsive). Fixed via `asyncio.to_thread(engine.run_cycle)` in `/api/engine/scan`.
- SSE gotchas: EventSource can't set headers → JWT goes as `?token=` query param; SQLite returns naive datetimes → keep the feed watermark naive UTC too.
- `trades.user_id` is NOT NULL — seed trades must reference a real user (used admin@demo.com).

---

## Environment Notes (no secrets — see `.env`)

- Backend start: `python -m uvicorn backend.main:app --host 127.0.0.1 --port 8001`
- Port 8001 (8000 had TIME_WAIT issues); frontend expects `NEXT_PUBLIC_API_URL` or defaults to `http://127.0.0.1:8001`
- Dashboard DB config uses `DASHBOARD_DATABASE_URL` (not `DATABASE_URL`) to avoid clashing with the trading system's PostgreSQL URL
- pip requires `--index-url https://pypi.org/simple/` on this machine
- `bcrypt==4.1.3` pinned (5.0 incompatible with passlib)
- npm hangs on this machine (Defender blocks node_modules extraction) — run installs on server or manually
- Binance API blocked from this machine; Telegram API intermittent — both work on a real server
- Unicode chars can crash Python print on cp1252 console — keep scripts ASCII-safe
- Alpaca paper account: `SQ` ticker renamed to `XYZ`
- Alpaca DNS (`data.alpaca.markets`) intermittently fails to resolve on this machine — quote endpoints degrade to `null` by design; not a code bug

## Verification Scripts

- `logs/test_phase5.py` — Phase 5 end-to-end flow (register → scan → list → tier filter)
- `%TEMP%\opencode\test_phase7.py` — Phase 7 checks (decay, detail, accuracy, SSE live delivery)
- `%TEMP%\opencode\test_phase8.py` — Phase 8 checks (positions/holdings/performance, per-user isolation)
- `%TEMP%\opencode\test_phase10.py` — Phase 10 checks (status/exposure/limits, kill-switch blocking, max-positions + position-size gates, admin reset)
- `%TEMP%\opencode\test_phase11.py` — Phase 11 checks (AI analysis/summary/models, exchange connect/list/mask/test/disconnect, ownership isolation)
- `%TEMP%\opencode\test_phase12.py` — Phase 12 checks (plans/promos/invoices, approve-reject-refund lifecycle, renewal stacking, tier sync, stats, history filters)
- \%TEMP%\\opencode\\test_phase13.py\ - Phase 13 checks (prefs CRUD/validation, telegram link/test/unlink, tier gating, billing+engine dispatch hooks)
- \%TEMP%\\opencode\\test_phase14.py\ - Phase 14 checks (meta registry, validate-on-connect, ownership isolation, account snapshot 502 path, Kraken sig + Coinbase JWT unit tests)
- `%TEMP%\opencode\test_phase15.py` - Phase 15 checks (list/unread-count, event-driven creation via kill-switch/trade-close/invoice-approve, mark read/all/clear, type filter, ownership isolation)
- `%TEMP%\opencode\test_phase16.py` - Phase 16 checks (security headers, CORS origin rules, login lockout, JWT tamper rejection, SQLi smoke payloads, static raw-SQL scan)
- \%TEMP%\\opencode\\restart_backend.ps1\ / estart_frontend.ps1\ — safe server restarts: kill-by-port (netstat/taskkill), WMI-detached launch (no session pipe inheritance), time-boxed health polls — fixes the recurring shell freezes
